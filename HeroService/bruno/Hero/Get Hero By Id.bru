meta {
  name: Get Hero By Id
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/hero/{{heroId}}
  body: none
  auth: none
}

tests {
  test("should return status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("should return hero with correct id", function() {
    const body = res.getBody();
    expect(body).to.have.property('id');
    expect(body).to.have.property('name');
    expect(body).to.have.property('healthPoints');
  });
}