meta {
  name: Add Item to Inventory
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/hero/{{heroId}}/inventory/add
  body: json
  auth: none
}

body:json {
  {
    "id": 1,
    "quantity": 3
  }
}

tests {
  test("should return status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("should return updated hero", function() {
    const body = res.getBody();
    expect(body).to.have.property('inventory');
  });
}